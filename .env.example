# Not used directly — credentials are injected via 1Password CLI.
# See .env.op for vault references. Run tests with: npm test
# Requires: op CLI installed and authenticated (brew install 1password-cli)
